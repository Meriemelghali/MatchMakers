spring.application.name=FinanceService

server.port=8091
server.servlet.context-path=/finance

# IMPORTANT: do not commit credentials. Configure via env var for Atlas; defaults to local MongoDB for dev.
spring.data.mongodb.uri=${MONGODB_URI_FINANCE:mongodb://localhost:27017/financedb}
spring.data.mongodb.auto-index-creation=true
