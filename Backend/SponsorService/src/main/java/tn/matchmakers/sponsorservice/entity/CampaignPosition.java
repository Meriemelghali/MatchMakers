package tn.matchmakers.sponsorservice.entity;

public enum CampaignPosition {
    TOP,      // en haut de la liste
    FEATURED, // carte mise en avant
    BANNER,   // bannière pub
    SIDEBAR   // côté
}