spring.application.name=ProductService

server.port=8092
server.servlet.context-path=/products

# IMPORTANT: do not commit credentials. Configure via env var for Atlas; defaults to local MongoDB for dev.
spring.data.mongodb.uri=${MONGODB_URI_PRODUCTS:mongodb://localhost:27017/productsdb}
spring.data.mongodb.auto-index-creation=true
