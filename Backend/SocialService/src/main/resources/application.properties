spring.application.name=SocialService

server.port=8090
server.servlet.context-path=/social

# IMPORTANT: do not commit credentials. Configure via env var for Atlas; defaults to local MongoDB for dev.
spring.data.mongodb.uri=${MONGODB_URI_SOCIAL:mongodb://localhost:27017/socialdb}
spring.data.mongodb.auto-index-creation=true
