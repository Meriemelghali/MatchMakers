spring.application.name=SportService

server.port=8084
server.servlet.context-path=/sports

# IMPORTANT: do not commit credentials. Configure via env var for Atlas; defaults to local MongoDB for dev.
spring.data.mongodb.uri=${MONGODB_URI_SPORTS:mongodb://localhost:27017/sportsdb}
spring.data.mongodb.auto-index-creation=true
