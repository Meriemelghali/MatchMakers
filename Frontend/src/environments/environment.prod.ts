export const environment = {
    production: true,
    matchServiceUrl: '/matchs',
    terrainServiceUrl: '/terrain',
    // If you deploy the Python AI service behind the same domain, set it here (ex: '/ai').
    aiServiceUrl: ''
};
